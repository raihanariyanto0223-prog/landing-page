[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Skip Next.js auto-detection since this is a Vite project
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  # Match the environment
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
